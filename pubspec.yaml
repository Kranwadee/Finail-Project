name: project
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0' # SDK constraint ที่ถูกต้อง

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.0
  cached_network_image: ^3.0.0
  cupertino_icons: ^1.0.6
  sqflite: ^2.3.3+1
  provider: ^6.1.2 # เปลี่ยนเป็น ^ เพื่อให้รับการอัปเดต
  path: any
  fluttertoast: ^8.2.8
  http: ^1.2.2
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  flutter_dotenv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  assets:
    - assets/img/
  uses-material-design: true
